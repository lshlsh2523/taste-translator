# design.md — 이런느낌 (taste-translator)

메인 페이지 히어로 화면 명세. 이 문서는 **방향과 제약**을 정한다. 명시되지 않은 세부(정확한 여백 값, 애니메이션 이징, 클래스 구조)는 판단해서 결정할 것.

---

## 1. 무엇을 만드는가

사용자가 원하는 제품의 인상을 자연어로 입력하면, 취향 어휘와 럭셔리 업계 용어로 옮겨 실제 MCM 제품과 연결하는 서비스. 이 화면은 그 서비스의 첫 화면이다.

**이 화면의 단 하나의 목적**: 방문자가 "내 얘기다"라고 느끼고 검색 페이지로 넘어가게 하는 것.

**타깃**: 대중 브랜드에서 럭셔리로 처음 넘어가는 20대. 원하는 인상은 분명한데 그것을 부를 이름이 없는 상태.

**MCM 공식 서비스가 아니다.** 공식몰과 시각적으로 인접하되, 오인될 만큼 닮으면 안 된다.

---

## 2. 레이아웃

좌우 2단. 데스크톱 기준 좌측 텍스트 약 45%, 우측 이미지 약 55%. 전체 뷰포트 높이(`100svh`)를 채우고, 스크롤 없이 한 화면에서 끝난다.

```
┌──────────────────────────┬───────────────────────────────┐
│                          │                               │
│                          │        ┌───────────┐          │
│  이런 느낌,               │        │           │          │
│  뭐라고 부르지            │        │  고양이    │          │
│                          │        │  2마리     │          │
│  원하는 인상은 분명한데    │        │  (누끼)    │          │
│  부를 이름이 없어서       │        │           │          │
│  검색을 시작하지 못하는   │        │  화면 대비 │          │
│  사람들을 위해.           │        │  크게      │          │
│                          │        │           │          │
│  문장으로 설명하면,       │        └───────────┘          │
│  그 취향에 이름을 붙여    │                               │
│  실제 MCM 제품까지        │   ← 커서 리빌 영역             │
│  연결합니다.              │                               │
│                          │                               │
│  [ 내 취향 찾기 → ]       │                               │
│                          │                               │
├──────────────────────────┴───────────────────────────────┤
│  이 서비스는 MCM 공식 서비스가 아니며, 학생 프로젝트입니다.  │
└──────────────────────────────────────────────────────────┘
```

좌측 텍스트 블록은 수직 중앙 정렬, `max-width` 520px 내외.

고지문은 좌측 컬럼 하단이 아니라 **화면 최하단 전체 폭**에 둔다.

---

## 3. 카피 (확정 — 변경 금지)

**헤드라인**
```
이런 느낌,
뭐라고 부르지
```
2줄 고정. 물음표 없음 — 질문이 아니라 혼잣말이다.

**서브카피**
```
원하는 인상은 분명한데 부를 이름이 없어서
검색을 시작하지 못하는 사람들을 위해.

문장으로 설명하면, 그 취향에 이름을 붙여
실제 MCM 제품까지 연결합니다.
```
두 문단. 줄바꿈 위치도 그대로 유지.

**CTA**
```
내 취향 찾기 →
```
버튼 1개만. 보조 버튼 두지 말 것. `/search`로 이동.

**고지문**
```
이 서비스는 MCM 공식 서비스가 아니며, 학생 프로젝트로 제작되었습니다.
```

**AI를 주어로 쓰지 말 것.** 이 서비스는 취향을 판정하지 않고 번역한다. "AI가 분석해드립니다" 류의 문구를 추가하지 말 것.

---

## 4. 고양이 이미지

**파일**: `public/images/cats.webp` (투명 배경, 누끼 완료 상태)

**크기가 이 화면의 핵심이다.** 우측 영역에서 소극적으로 놓지 말고, **화면 높이의 2/3 이상**을 차지하도록 크게 배치한다. 하단이 뷰포트 아래로 살짝 잘려나가도 무방하다 — 오히려 스케일감이 산다.

- 하단 정렬(`bottom: 0` 기준). 위쪽에 여백이 남고 아래는 꽉 차게
- 우측 가장자리로 살짝 붙여도 좋음. 정중앙 정렬은 심심하다
- 배경이 투명하므로 페이지 배경 위에 그대로 얹힌다. 별도 프레임·카드·그림자 금지
- `next/image` 사용, `priority` 지정 (첫 화면 LCP 요소)

**모바일**: 좌우 분할이 무너지므로 텍스트 위 / 이미지 아래 세로 배치. 이미지는 높이 40vh 내외로 축소하고 하단 잘림 유지.

---

## 5. 커서 리빌 (골격만 — 이번 단계에서는 미구현)

추후 고양이 이미지 위에 취향 용어 네임택이 얹히고, 커서가 지나간 자리에만 드러나는 인터랙션이 붙는다.

**이번 작업에서 준비할 것:**

- 이미지와 같은 좌표계를 공유하는 오버레이 레이어를 컴포넌트로 분리해둘 것 (`<CatReveal />` 등)
- 네임택 데이터는 배열로 외부에 빼둘 것. 좌표는 이미지 기준 퍼센트로:
  ```ts
  type NameTag = {
    ko: string;      // "사첼"
    en: string;      // "Satchel"
    x: number;       // 0~100 (이미지 폭 기준 %)
    y: number;       // 0~100
    side: "left" | "right";
  };
  ```
- 지금은 빈 배열로 두고 렌더만 통과시킬 것. 좌표는 추후 채운다

**구현 시 주의(추후용 메모)**: 마스크는 캔버스 `toDataURL()` 방식이 아니라 CSS `mask-image: radial-gradient(...)`로. 커서 좌표만 CSS 변수로 갱신한다. 데스크톱 전용.

---

## 6. 톤과 타이포그래피

**방향**: 사진 의존형 럭셔리 미니멀이 아니라 **타이포그래피 기반 에디토리얼**. 이 서비스가 다루는 것이 언어이기 때문이다. 잘 만든 인쇄물·저널에 가깝게.

**서체 3단**

| 역할 | 서체 | 비고 |
| --- | --- | --- |
| 본문·UI | Pretendard Variable | `next/font/local` |
| 국문 헤드라인 | 명조 계열 | 산돌 제공분 대기 중. 임시로 Gowun Batang |
| 영문 액센트 | Instrument Serif | 400 단일 굵기 |

⚠️ **Instrument Serif에 `font-weight: bold`를 걸지 말 것.** 400만 존재하므로 가짜 볼드가 생성되어 자획이 뭉개진다. 강조는 크기로.

⚠️ **Inter 및 라틴 전용 산세리프 금지.** 한글 글리프가 없어 환경마다 폴백이 달라진다.

**헤드라인**은 국문 명조, 화면에서 가장 큰 요소. 행간 좁게(1.15~1.2), 자간 살짝 좁게. 이 화면에서 가장 먼저 읽혀야 한다.

**영문 액센트의 자리**: 지금 단계에서는 거의 쓸 곳이 없다. 억지로 넣지 말 것. 네임택이 붙을 때 `Satchel`, `Hobo` 같은 원어 표기에서 본격적으로 쓰인다.

---

## 7. 컬러

MCM이 공식적으로 명시한 브랜드 컬러는 블랙 하나뿐이다. 코냑은 브랜드 컬러가 아니라 소재 이름이며 공식 hex가 존재하지 않는다.

```
배경    따뜻한 오프화이트 — 고양이 이미지 배경톤과 이어지도록
잉크    거의 블랙 (순수 #000보다 살짝 부드럽게)
보조    회색 계열 2단 (서브카피용 / 고지문용)
선      아주 옅은 웜 그레이 헤어라인
```

정확한 값은 판단해서 정할 것. 단, **고양이 이미지의 웜 오프화이트 배경톤과 페이지 배경이 자연스럽게 이어져야** 한다. 누끼 이미지라 배경이 비치므로 톤이 어긋나면 오려붙인 티가 난다.

**채도 있는 색을 UI에 쓰지 말 것.** 색은 사진(고양이 옷의 데님 블루) 안에만 존재한다.

---

## 8. 금지 사항

생성형 도구가 기본값으로 만들어내는 것들. 럭셔리 톤을 가장 빠르게 무너뜨린다.

- 보라→파랑 그라디언트, 모든 종류의 그라디언트
- 드롭섀도우, 글로우, 블러, 글래스모피즘
- 12px을 넘는 `border-radius`
- 카드 안의 카드
- 제목 위 라운드 사각 아이콘 타일
- 이모지 아이콘
- 컬러 배경 위 회색 텍스트
- 스크롤 하이재킹
- 3D / WebGL / 무거운 스크롤 애니메이션
- MCM 로고, 비세토스 모노그램, 브랜드 식별 요소 일체

---

## 9. 모션

이 화면에서는 **최소한만**. 과한 애니메이션은 AI가 만든 티를 낸다.

허용: 페이지 진입 시 좌측 텍스트의 절제된 순차 페이드인, CTA 호버 반전, 포커스 링.

`prefers-reduced-motion: reduce`를 반드시 존중할 것.

---

## 10. 기술 제약

- Next.js App Router / TypeScript / Tailwind CSS v4
- 전체 한국어 UI. 타이포 스케일과 행간은 한글 기준으로 잡을 것
- 반응형 필수. 모바일에서 무너지면 안 됨
- 키보드 포커스 가시성 확보
- 첫 화면 로딩 속도가 곧 평가 대상. 이미지 최적화 필수

---

## 11. 이번 작업 범위

**포함**: 히어로 화면 1개 (좌측 텍스트 + 우측 고양이 + 고지문), 반응형, 컬러·타이포 토큰 정의

**제외**: 네임택 좌표와 커서 리빌 구현, 검색 페이지, 결과 페이지

컴포넌트는 추후 네임택 레이어를 얹기 쉽게 분리해둘 것.